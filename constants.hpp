#pragma once

#include <cstdint>

namespace Constants {
inline constexpr uint8_t EMPTY_ASCII = 0x2D;
inline constexpr double DELAY_PER_BYTE_MS = 1200.0 / 1200.0;
inline constexpr double PRINT_INTERVAL = 10.0;
} // namespace Constants
